import { AccessToken } from './ges-api';
export interface Config {
    username: string;
    access_token: string;
    token_type: string;
    expires: number;
}
export declare function prompt_credentials(): Promise<AccessToken & {
    username: string;
}>;
export declare function save(config: Config): Promise<void>;
export declare function load(exit_if_not_logged?: boolean): Promise<Pick<Config, 'access_token' | 'token_type'>>;
export declare function erase(): Promise<void>;
